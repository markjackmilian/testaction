name: Webhook Action

on:
  push:
    branches:
      - '*'

jobs:
  webhook:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Logging
        run: |
          echo "${{toJSON(github.event)}}"

     
